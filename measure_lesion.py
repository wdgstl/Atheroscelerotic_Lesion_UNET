#preprocess hist image (resize w aspect ratio)

#get UNET inference (model.predict) to get segementation

#pass mask into post_processing to get lesion measurements

#ouput mask labeled with measurements
